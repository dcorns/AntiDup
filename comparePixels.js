//import Pixelmatch from "pixelmatch";

const comparePixels = (image1, image2, width, height) => {
  //const diff = Pixelmatch(image1, image2, null, width, height, {
  // threshold: 0.1,
  // });
  return 0;
  //return diff;
};

export default comparePixels;
